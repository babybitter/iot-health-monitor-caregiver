{
  "name": "iot-health-monitor-caregiver",
  "version": "1.0.0",
  "private": true,
  "description": "WeChat Mini Program for caregiver duty, patient monitoring and care tasks",
  "scripts": {
    "lint": "node scripts/validate-project.js",
    "test": "node scripts/run-tests.js",
    "build": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}

